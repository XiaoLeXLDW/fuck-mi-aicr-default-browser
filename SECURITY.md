# 安全问题报告

当前仅关注仓库最新源码的安全修复；没有稳定版支持周期或响应时限承诺。

如仓库的 **Security → Advisories → Report a vulnerability** 可用，请使用该入口私下报告。
若尚未启用，只提交不含利用细节和敏感数据的 Issue，请维护者提供私下接收方式。
这里没有预设或虚构维护者邮箱。

报告请包含受影响版本、权限后端、最小复现、实际影响和建议修复方向。不要上传真实访问网址、
登录令牌、签名密钥、配对信息或未经脱敏的完整日志。

## 权限边界

应用依赖由用户授权的 Shizuku / Stellar 特权服务，UserService 可能运行在 shell 或 root 身份。
控制器能够接收系统 Activity 事件，当前实现仅尝试处理 `com.android.browser` 的网页启动。
全局控制器冲突、目标浏览器启动失败和 ROM 限制详见 [README](README.md)。

源码没有实现遥测或上传服务；普通 App 的网络权限与特权进程能力不能混为一谈。
数据处理和系统日志边界见[隐私说明](docs/PRIVACY.md)。
