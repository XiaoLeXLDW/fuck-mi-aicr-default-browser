# 岛外打开 v0.3.4 · Universal APK

实验性预发布版本，最低 Android 8.0（API 26）；包名 `dev.codex.mibrowserredirector`，`versionCode 11`。
[下载 APK 与校验文件](https://github.com/XiaoLeXLDW/fuck-mi-aicr-default-browser/releases/tag/v0.3.4)。

## 更新内容

- 页首使用中性的“网页重定向”，观察模式提示明确关闭开关后需再次点“开启接管”应用设置。
- 浏览器要求、权限服务启动、身份校验、停止和系统拒绝注册的提示与实际行为一致。
- 图标声明与当前资源、APK 内副本同步，隐私说明正确描述随机 UI 转储文件和尽力删除机制。
- 用户文档清理制作过程文字，保留资源来源、许可边界与维护者设计档案。
- Wiki 导出支持跨目录链接和固定源码提交，发布证据链接保持版本对应关系。

本版未改变接管、后台服务或权限协议的处理逻辑。

## 安装与升级

附件为 `MiBrowserRedirector-v0.3.4-universal.apk` 与同名 `.sha256`。
沿用既有测试证书；升级前在旧版点“停用并退出服务”，等待“停止完成”，再安装新版。
同包名、同签名且版本号递增满足覆盖升级条件，本版尚未在手机验证安装过程。

| 项目 | 值 |
| --- | --- |
| 包名 / 版本 | `dev.codex.mibrowserredirector` / `0.3.4 (11)` |
| 服务身份 | 协议 `200` / 服务代 `30005` |
| 证书 SHA-256 | `24211beec19c121f4135640e9c95c75c5b981726b3172b7658e169f28894d376` |
| 最终 APK 大小与 SHA-256 | 见本版本 Release 正文及同名校验附件 |

应用保持原包名，APK 内含项目与第三方许可证/声明。已发布的 v0.3.3 标签和附件不变。

## 验证边界

本地 Debug / Release 各 119 项 Java 单测通过，原生会话 harness 14 项通过，共 133 项不同 Java 用例。
离线停服/子进程/版本字段回归 30 项、Wiki 导出回归 17 项通过；Lint 为 0 错误、11 警告。
Wiki 回归调用真实脚本并检查导出文件，另已核对实际项目 9 页的 GitHub 与本地预览链接。
版本字段检查只核对声明的版本信息，不等于所有文案、远端设置或设备兼容性已验证。
最终构建提交、测试结果和附件核验记录见本版本 Release 正文。

本版没有手机或模拟器验收。Shizuku / Stellar 实际授权、跳转、停服和重开，以及 HyperOS
图标、页面、折叠/大字体显示仍待确认，因此继续标为 **Pre-release**。
不修改系统默认浏览器，不处理 WebView；目标浏览器启动失败不自动回退。
接管使用全局 Activity Controller，不能与其他控制器工具同时运行。
